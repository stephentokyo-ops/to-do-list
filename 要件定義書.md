# Todoアプリ 要件定義書

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | シンプルTodoアプリ |
| プラットフォーム | Webアプリケーション |
| 技術スタック | React + TypeScript |
| データ保存 | ブラウザのlocalStorage |
| 対象ユーザー | 個人利用（シングルユーザー） |

## 2. 機能要件

### 2.1 Todoの追加
- テキスト入力欄からTodoを追加できる
- 空文字のTodoは追加できない（バリデーション）
- 追加後、入力欄はクリアされる
- 各Todoには一意のID、タイトル、完了状態、作成日時が付与される

### 2.2 Todoの完了/未完了の切り替え
- チェックボックスで完了/未完了を切り替えられる
- 完了済みのTodoは視覚的に区別される（取り消し線など）

### 2.3 Todoの削除
- 各Todoに削除ボタンを配置する
- クリックでTodoを即時削除する

### 2.4 Todo一覧の表示
- 登録されたTodoをリスト形式で表示する
- Todoが0件の場合、空状態のメッセージを表示する

### 2.5 データの永続化
- localStorageを使用してデータを保存する
- ブラウザをリロードしてもデータが保持される

## 3. 非機能要件

### 3.1 パフォーマンス
- 初期表示は1秒以内
- Todo追加・削除・切り替えの操作は即座に反映される

### 3.2 対応ブラウザ
- Chrome（最新版）
- Firefox（最新版）
- Safari（最新版）
- Edge（最新版）

### 3.3 レスポンシブ対応
- PC（1024px以上）
- タブレット（768px〜1023px）
- スマートフォン（767px以下）

## 4. データモデル

```typescript
interface Todo {
  id: string;        // 一意の識別子（UUID）
  title: string;     // Todoのタイトル
  completed: boolean; // 完了状態
  createdAt: string;  // 作成日時（ISO 8601形式）
}
```

## 5. 画面構成

```
┌─────────────────────────────┐
│         Todoアプリ           │  ← ヘッダー
├─────────────────────────────┤
│ [  新しいTodoを入力...  ][追加] │  ← 入力エリア
├─────────────────────────────┤
│ ☐ 牛乳を買う           [×]  │  ← Todoアイテム
│ ☑ メールを送る（完了）    [×]  │  ← 完了済みアイテム
│ ☐ レポートを書く        [×]  │
├─────────────────────────────┤
│       全3件（完了: 1件）      │  ← フッター（件数表示）
└─────────────────────────────┘
```

## 6. 技術構成

```
todo-app/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── TodoForm.tsx      # 入力フォーム
│   │   ├── TodoList.tsx      # 一覧表示
│   │   ├── TodoItem.tsx      # 個別アイテム
│   │   └── TodoFooter.tsx    # フッター（件数表示）
│   ├── hooks/
│   │   └── useTodos.ts       # Todo操作のカスタムフック
│   ├── types/
│   │   └── todo.ts           # 型定義
│   ├── utils/
│   │   └── storage.ts        # localStorage操作
│   ├── App.tsx
│   ├── App.css
│   └── main.tsx
├── package.json
└── tsconfig.json
```

## 7. 主要な技術選定

| カテゴリ | 技術 | 理由 |
|---------|------|------|
| 言語 | TypeScript | 型安全性の確保 |
| UIライブラリ | React 19 | コンポーネントベースの開発 |
| ビルドツール | Vite | 高速なビルド・開発サーバー |
| スタイリング | CSS Modules | スコープ付きCSS、追加依存なし |
| ID生成 | crypto.randomUUID() | 標準API、依存なし |

## 8. 受け入れ基準

- [ ] Todoを追加できる
- [ ] 空文字ではTodoを追加できない
- [ ] Todoの完了/未完了を切り替えられる
- [ ] 完了済みTodoに取り消し線が表示される
- [ ] Todoを削除できる
- [ ] ブラウザリロード後もデータが残る
- [ ] Todo件数が正しく表示される
- [ ] レスポンシブデザインで表示される
